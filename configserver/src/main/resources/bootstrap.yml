spring:
  application:
    name: config-server
  profiles:
    active:
      - native
  cloud:
    config:
      server:
        #local conf: this locations can either of classpath or locations in the filesystem
        native:
          search-locations: classpath:/config
server:
  port: 8071

encrypt:
  key: v8y/B?D(G+Kb